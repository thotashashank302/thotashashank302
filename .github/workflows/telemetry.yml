name: Update Mission Telemetry

on:
  schedule:
    - cron: '17 3 * * *'   # daily at 03:17 UTC — change as you like
  workflow_dispatch: {}     # lets you trigger it manually from the Actions tab
  push:
    branches: [main]
    paths:
      - 'scripts/generate_telemetry.py'

permissions:
  contents: write

jobs:
  telemetry:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Generate telemetry SVG
        env:
          GH_TOKEN: ${{ secrets.TELEMETRY_TOKEN }}
          GH_USERNAME: ${{ github.repository_owner }}
        run: python scripts/generate_telemetry.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add assets/telemetry.svg
          git diff --staged --quiet || git commit -m "chore: refresh mission telemetry"
          git push
